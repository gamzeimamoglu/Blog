@model Entity.Models.EmailSubscription

<div class="well">
    <h2>Abonelik</h2>
    <p>Yeni </p>
        @using (Html.BeginForm())
        {
            <div class="input-group">
                @Html.TextBoxFor(x=>x.Email, new { @class="form-control" })
                <span class="input-group-btn">
                    <button class="btn btn-default emailSave" type="submit">Go!</button>
                </span>
            </div>
        }
</div>